<br />
<%= link_to("←回工作列表", jobs_path )  %>

<br />
<br />
<h3><%= @job.title %></h3>
<br />

<%= @job.created_at %>
<p>
  <%= simple_format(@job.description) %>
</p>

<div class="text-center">
  <%= link_to("提交简历", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration:underline;") %>
</div>
